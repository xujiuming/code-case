spring:
 cloud:
     config:
       profile: dev
       label: master
       #username: ming
       #password: ming
       #uri: "http://localhost:7001"
       #开启 以服务方式访问配置中心
       discovery:
         enabled: true
         service-id: CONFIG
 application:
   name: common
       #fail-fast: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:10002/eureka/
